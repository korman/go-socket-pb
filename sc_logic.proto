syntax = "proto3";

option go_package = "../server";

package server;

import "pb/global_define.proto";

message OtherInputText {
  repeated InputText inputText = 1;
}

message UpdateNodes {
  MapInfo map = 1;
  repeated Node lockNodes = 2;
}